
// This file is kept for potential future server-side logic if needed.
// The actual client-side dropdown interaction is in CategoryFilterClient.tsx
// For now, this component doesn't do much other than act as a placeholder.

import { EVENT_CATEGORIES } from "@/lib/constants";

export default function CategoryFilter() {
  // Server-side logic for categories, if any, could go here.
  // For now, it primarily serves to show where such logic would live.
  // The client component CategoryFilterClient.tsx handles the UI and interaction.
  return null; 
}
