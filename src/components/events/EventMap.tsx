
"use client";

import React from 'react';

// This is a placeholder component after rolling back Leaflet map integration.
// You can remove this file if it's no longer needed or re-implement map functionality later.
export default function EventMapComponent() {
  return (
    <div className="flex h-full w-full items-center justify-center text-muted-foreground p-4 bg-muted rounded-lg">
      Map functionality has been rolled back.
    </div>
  );
}
